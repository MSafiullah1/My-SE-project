{
    "name": "succession-planning-app",
    "version": "1.0.0",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "start": "npn run dev",
        "build": "npm run build:client",
        "build:client": "cd Proj/client && npm install && npm install --save-dev vite && npm run build"
    }
}
