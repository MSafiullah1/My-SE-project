{
    "name": "succession-planning-app",
    "version": "1.0.0",
    "engines": {
        "node": "20.x"
    },
    "scripts": {
        "build": "vite build",
        "start": "npm run dev",
        "start:client": "cd Proj/client&& npm install && npm run build"
    }
}
